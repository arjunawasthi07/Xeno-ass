{
  "name": "crm-backend",
  "version": "1.0.0",
  "description": "CRM backend service",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "production": "node server.js",
    "start-all": "concurrently --kill-others \"npm start\" \"npm start --prefix ../channel-service\" \"npm run dev --prefix ../frontend\""
  },
  "dependencies": {
    "@google/genai": "^2.8.0",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "pg": "^8.21.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
